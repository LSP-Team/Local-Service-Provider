package com.techfinder.localserviceprovider.ui.theme

import androidx.compose.ui.graphics.Color

// BASE SURFACES
val AppBackground = Color(0xFFF8FAFC)    // Crisp Off-White/Slate
val SurfaceCard = Color(0xFFFFFFFF)      // Pure White Cards/Inputs
val SurfaceRaised = Color(0xFFF1F5F9)    // Subtle Gray-Blue Container
val SurfaceHighlight = Color(0xFFE2E8F0) // Interactive Surface Hover

// PRIMARY — INDIGO BLUE (Trust & Professionalism)
val PrimaryBlue = Color(0xFF2563EB)      // Vibrant Primary
val PrimaryBlueDark = Color(0xFF1D4ED8)  // Pressed State
val PrimaryBlueLight = Color(0xFFEFF6FF) // Light Blue Container Fill
val PrimaryBlueGlow = Color(0x2B2563EB)  // Soft Focus Ring Shadow

// ACCENT — EMERALD (Success & Service Ready)
val AccentTeal = Color(0xFF10B981)
val AccentTealDark = Color(0xFF047857)
val AccentTealLight = Color(0xFFECFDF5)

// SECONDARY ACCENT — AMBER
val AccentAmber = Color(0xFFF59E0B)
val AccentAmberLight = Color(0xFFFEF3C7)

// STATUS COLORS
val StatusError = Color(0xFFEF4444)
val StatusErrorLight = Color(0xFFFEF2F2)
val StatusSuccess = Color(0xFF10B981)
val StatusSuccessLight = Color(0xFFECFDF5)
val StatusWarning = Color(0xFFF59E0B)

// BORDER / DIVIDER
val BorderSubtle = Color(0xFFE2E8F0)
val BorderDefault = Color(0xFFCBD5E1)    // Sharp, readable input borders
val BorderFocused = Color(0xFF2563EB)    // High-visibility focus state
val BorderStrong = Color(0xFF94A3B8)

// TEXT (High Contrast Accessibility)
val TextPrimary = Color(0xFF0F172A)      // Deep Slate Gray (Easier on eyes than #000)
val TextSecondary = Color(0xFF475569)    // Readable Subtitles/Labels
val TextTertiary = Color(0xFF64748B)     // Section Headers / Uppercase Labels
val TextHint = Color(0xFF94A3B8)         // Clear Placeholder Text

val TextOnPrimary = Color(0xFFFFFFFF)
val TextOnAccent = Color(0xFFFFFFFF)

// DISABLED STATES
val DisabledContainer = Color(0xFFE2E8F0)
val DisabledContent = Color(0xFF94A3B8)